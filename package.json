{
  "name": "tyrannobot",
  "version": "1.0.0",
  "description": "Bot for tyrant quotes",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "clean": "del tyrannobotcore.zip",
    "build": "7z a tyrannobotcore.zip loaders resources handlers *.js",
    "deploy": "aws lambda update-function-code --function-name TyrannoBot --zip-file fileb://tyrannobotcore.zip --publish",
    "chaindeploy": "npm run clean && npm run build && npm run deploy"
  },
  "repository": {
    "type": "git",
    "url": "git@personal:Karvaporsas/tyrannobot.git"
  },
  "keywords": [
    "bot",
    "telegram",
    "tyrant",
    "dictator"
  ],
  "author": "Touko Hjelt",
  "license": "MIT",
  "dependencies": {
    "aws-sdk": "^2.601.0",
    "request": "^2.88.0",
    "request-promise": "^4.2.5"
  }
}
